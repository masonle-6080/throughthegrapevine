#include <iostream>
#include <fstream>

#include <string>
#include <unordered_map>



void getInput(int &n, int &m, int &d){
    std::cin >> n >> m >> d;
    
}

int main() {
    
    int n, m, d;
    std::unordered_map<std

    getInput(n, m, d);

    


    // Test printing
    std::ofstream outFile("output.txt");

    outFile << "first line inputs: "<< n << " " << m << " " << d << "\n";
    return 0;
}